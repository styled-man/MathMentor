/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['https://your-domain.com', 'images.freeimages.com'], //make it 'your-domain.com'
  },
}

module.exports = nextConfig
